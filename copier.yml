# questions
plan:
  type: bool
  help: "There is an ambitious and realistic [development plan](../development_plan) for the virtual lab."

timeline:
    type: bool
    help: "There is a timeline for the co-development, validation of the virtual lab and development of training material."

codebase:
    type: bool
    help: "A new virtual lab is created."

version_control:
    type: bool
    help: "The codebase repository has version control (e.g. git)."

_answers_file: .my-custom-answers.yml
